name: CI

on: [push, pull_request]

jobs:
  build:
    runs-on: windows-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2.3.3

    - name: Install .NET Core
      uses: actions/setup-dotnet@v1.7.2
      with:
        dotnet-version: 3.1.x

    - name: Build & test
      run: dotnet test --configuration Release --logger GitHubActions

    - name: Build & publish
      run: dotnet publish LightBulbFR/ -o LightBulbFR/bin/Publish/ --configuration Release

    - name: Create installer
      run: |
        choco install innosetup --no-progress
        Copy-Item -Path LightBulbFR/bin/Publish/ -Recurse -Destination Installer/Source/
        & "c:\Program Files (x86)\Inno Setup 6\ISCC.exe" Installer/Installer.iss
      shell: pwsh

    - name: Upload build artifacts
      uses: actions/upload-artifact@v1
      with:
        name: LightBulbFR
        path: LightBulbFR/bin/Publish/

    - name: Upload build artifact (installer)
      uses: actions/upload-artifact@v1
      with:
        name: LightBulbFR-Installer
        path: Installer/bin/LightBulbFR-Installer.exe
